routes:
  unmatched: path
prometheus_export:
  port: 8999
otel_traces_export:
  endpoint: http://jaeger:4318
metrics:
  features:
    - application
attributes:
  kubernetes:
    cluster_name: obi-k8s-test-cluster
  select:
    "*":
      include: ["*"]